export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Provenant Serve</p>
        <h1>Scan GitHub repositories over HTTP</h1>
        <p>
          This front-end will provide a minimal UI for submitting repository
          scans to a locally running <code>provenant serve</code> backend.
        </p>
      </section>
    </main>
  )
}
